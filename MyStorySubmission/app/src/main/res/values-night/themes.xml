<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyStorySubmission" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/darkblu</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Base.Theme.ActionBar" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/darkblu</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="Theme.MyStorySubmission" parent="Base.Theme.MyStorySubmission" />

    <style name="TextStyle"/>

    <style name="TextStyle.TITLE">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/darkblu</item>
    </style>
</resources>